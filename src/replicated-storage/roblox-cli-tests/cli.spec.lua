return function()
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local RobloxCLI = require(ReplicatedStorage:FindFirstChild("roblox-cli"))

    --[[
        1) test good/bad inputs to Programs & etc params
        2) test running programs with mock cin cout and command
        3) test UI... as a demo
    ]]
end
